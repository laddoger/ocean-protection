<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ocean.protection.mapper.ForumPostMapper">
    <select id="selectAllPosts" resultType="com.ocean.protection.entity.ForumPost">
        SELECT 
            id,
            title,
            content,
            user_id,
            tag,
            image_url,
            images_json,
            created_time,
            updated_time,
            deleted,
            view_count,
            like_count,
            comment_count
        FROM post
        WHERE deleted = 0
        ORDER BY created_time DESC
    </select>

    <select id="selectByUserId" resultType="com.ocean.protection.entity.ForumPost">
        SELECT 
            id,
            title,
            content,
            user_id,
            tag,
            image_url,
            images_json,
            created_time,
            updated_time,
            deleted,
            view_count,
            like_count,
            comment_count
        FROM post
        WHERE user_id = #{userId}
        AND deleted = 0
        ORDER BY created_time DESC
    </select>
</mapper> 